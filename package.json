{
  "name": "stats-consume-api",
  "license": "MIT",
  "version": "0.1.0",
  "scripts": {
    "start": "sls offline",
    "vet": "go vet ./...",
    "watch": "nodemon --watch . -e go,yml --ignore '**/tmp/*' --exec 'npm run vet && npm run start'"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "serverless": "^3.36.0",
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-go-plugin": "^2.2.0",
    "serverless-offline": "^13.3.2"
  }
}
